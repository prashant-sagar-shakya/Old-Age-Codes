#include <bits/stdc++.h>
using namespace std;
int main()
{
    double a, b;
    cin >> a >> b;
    cout << std::fixed;
    cout << a / b << endl;
    cout << std::setprecision(3);
    cout << a / b;
    return 0;
}