#include <bits/stdc++.h>
using namespace std;
int main()
{
    auto a = 5; // change the value to change the datatype
    cout << "The size of " << typeid(a).name() << " is " << sizeof(a);
    return 0;
}