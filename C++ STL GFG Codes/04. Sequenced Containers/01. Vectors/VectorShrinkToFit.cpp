#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> v;
    int n, m;
    cin >> n >> m;
    v.resize(n);
    cout << v.size() << endl;
    v.resize(m);
    cout << v.size();
    return 0;
}